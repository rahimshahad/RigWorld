<% if msg_error.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(msg_error.errors.count, "error") %></h2>

    <ul>
      <% msg_error.errors.each do |error| %>
        <li><%= error.full_message %></li>
      <% end %>
    </ul>
  </div>
<% end %>